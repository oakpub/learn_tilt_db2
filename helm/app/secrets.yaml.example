# This is an example secrets file. Copy it to `secrets.yaml` and fill in your actual secrets.
# This file should NOT be committed to version control.

serviceA:
  database:
    # Password for Service A's database
    password: "password-a-dev"

serviceB:
  database:
    # Password for Service B's database
    password: "password-b-dev"
